A RESTful API used for accessing and interacting with the Collections database.
Once connection to the database you can use the CRUD operations in found in the routes folder depending on your authorisation level.
SCHEMA's which constrain what can be used under these CRUD operations can be found in the models folder.
All of these files are connected to the index.js.
Sensitive information such as passwords are protected under encryption.
A GUI is supplied to provied an easier interface for non-developers.